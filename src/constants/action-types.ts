export enum ActionTypes {
  selection = 'selection',
  line = 'line',
  text = 'text',
  simpleLine = 'simpleLine',
  area = 'area',
  image = 'image',
  capture = 'capture',
  ruler = 'ruler',
  eraser = 'eraser',
}
