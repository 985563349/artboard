export enum ActionTypes {
  line = 'line',
  text = 'text',
  simpleLine = 'simple-line',
  area = 'area',
  image = 'image',
  capture = 'capture',
  ruler = 'ruler',
  eraser = 'eraser',
  colorPicker = 'color-picker',
}
