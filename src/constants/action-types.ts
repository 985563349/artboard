export enum ActionTypes {
  selection = 'selection',
  line = 'line',
  text = 'text',
  simpleLine = 'simple-line',
  area = 'area',
  image = 'image',
  capture = 'capture',
  ruler = 'ruler',
  eraser = 'eraser',
}
